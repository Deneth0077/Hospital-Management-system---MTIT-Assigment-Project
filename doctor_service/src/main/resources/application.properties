# Server
server.port=8081

# application Name
spring.application.name=doctor-service

# MongoDB Configuration - Atlas (Shared with Ward Service)
spring.data.mongodb.uri=mongodb+srv://denethc545_db_user:wardpass123@ward-management-microse.rjldbui.mongodb.net/ward_management?retryWrites=true&w=majority&appName=Ward-Management-Microservice

# Logging
logging.level.com.hospital=DEBUG
logging.level.org.springframework.data.mongodb.core.MongoTemplate=DEBUG
spring.jpa.properties.hibernate.format_sql=true

# H2 Console (useful for debugging)
spring.h2.console.enabled=true
spring.h2.console.path=/h2-console

# SpringDoc / Swagger
springdoc.api-docs.path=/api-docs
springdoc.swagger-ui.path=/swagger-ui.html
springdoc.swagger-ui.operationsSorter=method

# CORS is handled in code — no additional config needed here